<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.f5.mapper.MagazineHotDetailMapper1">
     <select id="selectMagazineHotDetail1" parameterType="magazinehotcontent" resultType="magazinehotdetail1">
     	select * from magazinedetials where journalname=#{journalname} and pubyear=#{pubyear} and pubvolume=#{pubvolume} and pubphase=#{pubphase};
     </select>
</mapper>